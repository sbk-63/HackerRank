public static int diagonalDifference(List<List<Integer>> arr) {
    // Write your code here
        int j = arr.size()-1;
        int Answer = 0;
        for(int i = 0; i < arr.size(); i++){
        Answer += arr.get(i).get(i) - arr.get(i).get(j);
        j--;
        } 
        return Math.abs(Answer);
    } 
