    public static int birthdayCakeCandles(List<Integer> candles) {
        Collections.sort(candles);
        int n = candles.get(candles.size()-1);
        return (int)Collections.frequency(candles, n);
    }
