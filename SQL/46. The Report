SELECT 
    CASE WHEN g.grade >= 8 THEN s.name ELSE null END AS name, g.grade, marks
FROM 
    studentS S INNER JOIN grades g ON s.marks 
between 
    g.min_mark AND g.max_mark
ORDER BY g.grade DESC, s.name;
